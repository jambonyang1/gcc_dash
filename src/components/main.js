import React from "react";
import ReactDOM from "react-dom";
import styled from "styled-components";

const Main = () => {
  return (
    <div>
      <h1>메인 페이지</h1>
    </div>
  );
};

export default Main;
